"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = parseEntry;

var _path = _interopRequireDefault(require("path"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const resolvePath = relativeFilePathArray => relativeFilePathArray.map(entry => _path.default.resolve(process.cwd(), entry));

function parseMultipleEntries(entry) {
  if (Array.isArray(entry)) {
    return resolvePath(entry);
  }

  if (typeof entry === 'object' && entry !== null) {
    return Object.keys(entry).reduce((fileArray, key) => {
      const filesForEntry = entry[key];

      if (typeof filesForEntry === 'string') {
        fileArray.push(filesForEntry);
        return fileArray;
      }

      fileArray.push(`(chunk: ${key})`, ...resolvePath(filesForEntry));
      return fileArray;
    }, []);
  }

  return entry;
}

function parseEntry(entry) {
  if (typeof entry === 'string') {
    return _path.default.join(process.cwd(), entry);
  }

  if (Array.isArray(entry) || typeof entry === 'object' && entry !== null) {
    return parseMultipleEntries(entry).join('\n');
  }

  return entry;
}
//# sourceMappingURL=parseEntry.js.map