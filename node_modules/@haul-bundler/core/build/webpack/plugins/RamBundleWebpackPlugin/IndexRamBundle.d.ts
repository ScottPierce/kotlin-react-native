/// <reference types="node" />
import webpack from 'webpack';
import { Module } from './RamBundleWebpackPlugin';
declare type ModuleBuffer = {
    id: number;
    buffer: Buffer;
};
export default class IndexRamBundle {
    sourceMap: boolean;
    encoding: 'ascii' | 'utf16le' | 'utf8';
    header: Buffer;
    bootstrap: Buffer;
    toc: Buffer;
    modules: ModuleBuffer[];
    rawModules: Module[];
    constructor(bootstrap: string, modules: Module[], sourceMap?: boolean);
    private toNullTerminatedBuffer;
    private getOffset;
    private buildToc;
    build({ outputDest, outputFilename, sourceMapFilename, compilation, }: {
        outputDest: string;
        outputFilename: string;
        sourceMapFilename: string;
        compilation: webpack.compilation.Compilation;
    }): void;
}
export {};
//# sourceMappingURL=IndexRamBundle.d.ts.map