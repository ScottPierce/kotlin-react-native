import webpack from 'webpack';
export default class InitCoreDllPlugin {
    private setupFiles;
    private resolvedSetupFiles;
    constructor({ setupFiles }: {
        setupFiles: string[];
    });
    apply(compiler: webpack.Compiler): void;
}
//# sourceMappingURL=InitCoreDllPlugin.d.ts.map